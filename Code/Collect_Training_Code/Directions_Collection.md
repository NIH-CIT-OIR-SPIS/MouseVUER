# Directions For Collecting Training frames

Run the following command in this directory:
```
make clean && make -j4
```

Then run 
```
./run_record.sh
```

# For decompressing the frames to images run the following
```
./run_decompress.sh
```